# Booking App - Приложение для управления записями

Web-приложение для мастеров услуг и их клиентов с использованием Google Sheets в качестве backend.

## 🚀 Быстрый старт

1. Установите зависимости:
```bash
npm install
```

2. Настройте Google Sheets API (подробная инструкция в [SETUP.md](./SETUP.md)):
   - Создайте проект в Google Cloud Console
   - Включите Google Sheets API
   - Создайте Service Account
   - Скачайте JSON ключ
   - Создайте Google Sheet и поделитесь им с email Service Account

3. Создайте файл `.env.local` на основе `.env.example`:
```bash
cp .env.example .env.local
```

4. Заполните переменные окружения в `.env.local` (см. [SETUP.md](./SETUP.md))

5. Запустите приложение:
```bash
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:3000`
- Публичная форма записи: `http://localhost:3000/booking`
- Админка мастера: `http://localhost:3000/admin`

## 📁 Структура проекта

```
/src
 ├─ /components      # React компоненты
 ├─ /models          # Модели данных
 ├─ /services        # API сервисы
 ├─ /hooks           # Custom hooks
 ├─ /app             # Next.js App Router
 │   ├─ /admin       # Админка мастера
 │   └─ /booking     # Публичная форма записи
 └─ /utils           # Утилиты
```

## 🔐 Безопасность

- Все credentials хранятся в environment variables
- Service Account используется только на сервере
- Клиентская часть не имеет доступа к API ключам

## 📱 PWA

Приложение поддерживает установку как PWA на мобильных устройствах.

Для полноценной работы PWA необходимо создать иконки (см. `public/icon-placeholder.txt`).

## 🎯 Функциональность

### Админка мастера (`/admin`)
- Просмотр всех записей с сортировкой по дате
- Добавление новых записей
- Редактирование существующих записей
- Удаление записей
- Изменение статуса записи
- Фильтрация по источнику (клиент/мастер)

### Публичная форма записи (`/booking`)
- Простая форма для клиентов
- Выбор услуги, даты и времени
- Валидация обязательных полей
- Автоматическое создание записи со статусом "new"
- Экран успешной отправки

## 🛠 Технологии

- **Next.js 14** - React фреймворк с App Router
- **TypeScript** - типизация
- **Google Sheets API v4** - backend
- **Mobile-first CSS** - адаптивный дизайн
- **PWA** - поддержка установки как приложение

## 📝 Структура данных

Каждая запись в Google Sheet содержит:
- `id` - уникальный идентификатор
- `clientName` - имя клиента
- `phone` - телефон
- `service` - услуга
- `date` - дата (YYYY-MM-DD)
- `time` - время (HH:mm)
- `comment` - комментарий (опционально)
- `status` - статус: "new" | "confirmed" | "cancelled"
- `source` - источник: "client" | "master"

## 🔄 Миграция на другой backend

Код спроектирован для легкой замены backend:
- Сервис `GoogleSheetsService` инкапсулирует всю логику работы с данными
- API routes (`/api/records`) предоставляют единый интерфейс
- Для замены на Firebase/Supabase достаточно переписать `GoogleSheetsService`

## 📄 Лицензия

MIT
# clrb.lv-system
