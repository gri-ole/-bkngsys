# Инструкция по настройке приложения

## 1. Установка зависимостей

```bash
npm install
```

## 2. Настройка Google Sheets API

### Шаг 1: Создание проекта в Google Cloud Console

1. Перейдите на [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Включите **Google Sheets API**:
   - Перейдите в "APIs & Services" > "Library"
   - Найдите "Google Sheets API"
   - Нажмите "Enable"

### Шаг 2: Создание Service Account

1. Перейдите в "APIs & Services" > "Credentials"
2. Нажмите "Create Credentials" > "Service Account"
3. Заполните данные:
   - Name: `booking-app-service`
   - Description: `Service account for booking app`
4. Нажмите "Create and Continue"
5. Пропустите шаг "Grant this service account access to project" (нажмите "Done")

### Шаг 3: Создание ключа

1. Найдите созданный Service Account в списке
2. Откройте его и перейдите на вкладку "Keys"
3. Нажмите "Add Key" > "Create new key"
4. Выберите формат "JSON"
5. Скачайте файл ключа

### Шаг 4: Настройка Google Sheet

1. Создайте новый Google Sheet
2. Назовите его, например, "Booking Records"
3. В первой строке добавьте заголовки:
   ```
   A1: id
   B1: clientName
   C1: phone
   D1: service
   E1: date
   F1: time
   G1: comment
   H1: status
   I1: source
   J1: amount
   K1: paymentMethod
   ```
4. Откройте настройки доступа (кнопка "Share")
5. Добавьте email Service Account (из JSON файла, поле `client_email`)
6. Дайте права "Editor"
7. Скопируйте ID таблицы из URL:
   ```
   https://docs.google.com/spreadsheets/d/SPREADSHEET_ID/edit
   ```

## 3. Настройка переменных окружения

1. Скопируйте `.env.example` в `.env.local`:
   ```bash
   cp .env.example .env.local
   ```

2. Откройте `.env.local` и заполните:

   ```env
   # ID таблицы из URL Google Sheet
   GOOGLE_SHEETS_SPREADSHEET_ID=your_spreadsheet_id_here

   # Email Service Account из JSON файла
   GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@project.iam.gserviceaccount.com

   # Private key из JSON файла (всё содержимое поля private_key, включая \n)
   GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour private key here\n-----END PRIVATE KEY-----\n"

   # Диапазон данных (обычно не нужно менять)
   GOOGLE_SHEETS_RANGE=Sheet1!A2:K

   # Название приложения
   NEXT_PUBLIC_APP_NAME=Booking App

   # Путь к админке
   NEXT_PUBLIC_ADMIN_PATH=/admin

   # Пароль для админки (обязательно измените на безопасный!)
   ADMIN_PASSWORD=your_secure_password_here
   ```

   **Важно:** 
   - В `GOOGLE_PRIVATE_KEY` нужно сохранить все символы `\n` как есть
   - Если ключ в JSON файле выглядит как `"private_key": "-----BEGIN PRIVATE KEY-----\\n..."`, 
     то в `.env.local` должно быть `GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n..."`

## 4. Запуск приложения

```bash
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:3000`

- Публичная форма записи: `http://localhost:3000/booking`
- Админка мастера: `http://localhost:3000/admin`

## 5. Создание иконок для PWA (опционально)

Для полноценной работы PWA нужны иконки:

1. Создайте иконки размером 192x192 и 512x512 пикселей
2. Сохраните их как:
   - `public/icon-192.png`
   - `public/icon-512.png`

Можно использовать онлайн-генераторы или создать простые иконки в графическом редакторе.

## 6. Деплой

### Vercel

1. Установите Vercel CLI: `npm i -g vercel`
2. Запустите: `vercel`
3. Добавьте все переменные окружения из `.env.local` в настройках проекта на Vercel

### Другие платформы

Убедитесь, что все переменные окружения из `.env.local` добавлены в настройки вашей платформы.

## Структура Google Sheet

После настройки ваша таблица должна выглядеть так:

| id | clientName | phone | service | date | time | comment | status | source | amount | paymentMethod |
|----|------------|-------|---------|------|------|---------|--------|--------|--------|---------------|
| record-123... | Иван | +79991234567 | Стрижка | 2024-01-15 | 10:00 | | new | client | 25.00 | card |

Приложение автоматически заполняет данные при создании записей.

**Новые поля:**
- `amount` - сумма оплаты (число, опционально)
- `paymentMethod` - способ оплаты: `cash`, `card`, `online` или пусто

## 🔐 Аутентификация админки

Админка защищена паролем. Пароль настраивается в переменной окружения `ADMIN_PASSWORD`.

**Важно:** 
- Используйте надежный пароль
- Не коммитьте `.env.local` в git
- При деплое добавьте `ADMIN_PASSWORD` в переменные окружения вашей платформы

## 📊 Статистика

В админке доступна статистика:
- Общее количество записей
- Статистика по статусам (новые, подтвержденные, отмененные)
- Финансовая статистика (общая сумма, по способам оплаты)
- Популярные услуги
- Статистика по источникам записей
