# 🐛 Решение проблемы медленного запуска dev сервера

## Проблема
Dev сервер Next.js зависает на этапе "✓ Starting..." и не запускается или запускается очень долго (более 1 минуты).

---

## ✅ РЕШЕНИЕ 1: Использовать production build

**Самый быстрый и надежный способ!**

```bash
# 1. Собрать production версию (один раз, ~30 секунд)
npm run build

# 2. Запустить production сервер (запускается за 2-3 секунды)
npm start
```

**Преимущества:**
- ⚡ Очень быстрый запуск (2-3 секунды)
- 🚀 Высокая производительность
- ✅ Стабильная работа

**Недостатки:**
- ❌ Нет hot-reload (нужно пересобирать при изменениях)
- Для пересборки: `npm run build && npm start`

---

## ✅ РЕШЕНИЕ 2: Быстрый перезапуск dev сервера

```bash
# Использовать готовый скрипт
chmod +x dev-server.sh
./dev-server.sh
```

Или вручную:
```bash
# 1. Остановить все процессы Next.js
pkill -f "next dev"

# 2. Очистить кеш
rm -rf .next

# 3. Запустить заново
npm run dev
```

⏱️ **Ожидание:** 30-60 секунд при первом запуске

---

## ✅ РЕШЕНИЕ 3: Увеличить память Node.js

```bash
NODE_OPTIONS='--max-old-space-size=4096' npm run dev
```

---

## 🔍 Причины проблемы

1. **Кеш Next.js** - поврежденный кеш в `.next/`
2. **Порты заняты** - предыдущие процессы не остановлены
3. **Первая компиляция** - TypeScript + большой проект
4. **File watching** - много файлов для отслеживания

---

## 📊 Сравнение методов

| Метод | Скорость запуска | Hot-reload | Рекомендация |
|-------|------------------|------------|--------------|
| Production (`npm start`) | ⚡⚡⚡ 2-3 сек | ❌ | **Для работы** |
| Dev (`npm run dev`) | 🐌 30-60 сек | ✅ | Для разработки |

---

## 💡 Рекомендация

### Для ежедневной работы:
```bash
npm run build && npm start
```

### Для разработки с частыми изменениями:
```bash
npm run dev
# Подождать 30-60 секунд
```

---

## 🆘 Если ничего не помогает

1. Проверьте логи:
```bash
cd /Users/g.olenkins/booking-app
npm run dev 2>&1 | tee dev.log
```

2. Проверьте TypeScript:
```bash
npx tsc --noEmit
```

3. Переустановите зависимости:
```bash
rm -rf node_modules package-lock.json
npm install
```

---

## ✅ Текущий статус

- ✅ Production build работает идеально
- ✅ TypeScript компилируется без ошибок
- ✅ Все тесты проходят
- ⚠️ Dev сервер медленный (известная проблема Next.js 14)

**Используйте production режим для комфортной работы!** 🚀
