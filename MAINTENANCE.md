# 🔧 Руководство по обслуживанию ColorLab

**После деплоя: как управлять сайтом**

---

## 📋 Содержание

1. [Резервное копирование](#резервное-копирование)
2. [Обновление кода](#обновление-кода)
3. [Мониторинг](#мониторинг)
4. [Безопасность](#безопасность)
5. [Оптимизация](#оптимизация)
6. [Частые задачи](#частые-задачи)

---

## 💾 Резервное копирование

### Что нужно бэкапить:

1. **Код** (уже на GitHub ✅)
2. **Данные из localStorage** (записи, настройки)

### Экспорт данных

**Через админ-панель:**

```javascript
// Откройте консоль браузера (F12) на странице админки
// https://colorlab.lv/admin

// Экспорт всех данных
const backup = {
  records: localStorage.getItem('records'),
  services: localStorage.getItem('services'),
  workingHours: localStorage.getItem('workingHours'),
  contactInfo: localStorage.getItem('contactInfo'),
  exportDate: new Date().toISOString()
};

// Скачать файл
const blob = new Blob([JSON.stringify(backup, null, 2)], {type: 'application/json'});
const url = URL.createObjectURL(blob);
const a = document.createElement('a');
a.href = url;
a.download = `colorlab-backup-${new Date().toISOString().split('T')[0]}.json`;
a.click();
```

### Импорт данных

```javascript
// В консоли браузера на /admin
const input = document.createElement('input');
input.type = 'file';
input.accept = '.json';
input.onchange = e => {
  const file = e.target.files[0];
  const reader = new FileReader();
  reader.onload = event => {
    const backup = JSON.parse(event.target.result);
    
    // Восстанавливаем данные
    if (backup.records) localStorage.setItem('records', backup.records);
    if (backup.services) localStorage.setItem('services', backup.services);
    if (backup.workingHours) localStorage.setItem('workingHours', backup.workingHours);
    if (backup.contactInfo) localStorage.setItem('contactInfo', backup.contactInfo);
    
    alert('Данные восстановлены! Обновите страницу.');
    location.reload();
  };
  reader.readAsText(file);
};
input.click();
```

### Автоматический бэкап (рекомендация)

**Делайте бэкап:**
- 📅 **Еженедельно** - через экспорт (см. выше)
- 📅 **Перед обновлением** - всегда перед git push
- 📅 **После важных изменений** - изменение настроек, много новых записей

**Где хранить:**
- ☁️ Google Drive / Dropbox
- 💾 Локальный компьютер
- 📧 Отправить себе на email

---

## 🔄 Обновление кода

### Для Vercel (автоматически):

```bash
cd /Users/g.olenkins/booking-app

# 1. Внесите изменения в код
# 2. Коммит и пуш
git add .
git commit -m "Описание изменений"
git push origin main

# 3. Vercel автоматически задеплоит новую версию (1-2 минуты)
# 4. Проверьте на https://colorlab.lv
```

### Для VPS (вручную):

```bash
# Подключение к серверу
ssh your_username@your_server_ip

# Переход в директорию проекта
cd /var/www/colorlab

# Получение новых изменений
git pull origin main

# Установка новых зависимостей (если были добавлены)
npm install

# Пересборка
npm run build

# Перезапуск
pm2 restart colorlab-booking

# Проверка логов
pm2 logs colorlab-booking --lines 50
```

---

## 📊 Мониторинг

### Vercel Dashboard

**Что можно отслеживать:**
- 🚀 **Деплои** - история всех обновлений
- 📈 **Analytics** - посещаемость, популярные страницы
- 🐛 **Logs** - ошибки и предупреждения
- ⚡ **Performance** - скорость загрузки

**Как проверить:**
1. Зайдите на https://vercel.com
2. Откройте проект `colorlab-booking`
3. Вкладки: **Deployments**, **Analytics**, **Logs**

### VPS Мониторинг

```bash
# Статус приложения
pm2 status

# Использование ресурсов
pm2 monit

# Логи (последние 100 строк)
pm2 logs colorlab-booking --lines 100

# Логи Nginx
sudo tail -f /var/log/nginx/access.log
sudo tail -f /var/log/nginx/error.log

# Использование диска
df -h

# Использование памяти
free -h

# CPU и процессы
htop
```

### Проверка работоспособности

**Ежедневно:**
- ✅ Откройте https://colorlab.lv/booking
- ✅ Создайте тестовую запись
- ✅ Проверьте получение email

**Еженедельно:**
- ✅ Проверьте все страницы админки
- ✅ Проверьте логи на ошибки
- ✅ Проверьте скорость загрузки (должна быть <2 сек)

---

## 🔒 Безопасность

### Пароль админ-панели

**Изменить пароль:**

1. Откройте https://colorlab.lv/admin
2. Войдите с текущим паролем
3. Перейдите в **Настройки** → **Безопасность**
4. Измените пароль
5. Сохраните

**Или через код:**

```bash
# В вашем проекте
cd /Users/g.olenkins/booking-app

# Откройте файл с паролем (если он хранится в коде)
# Обычно это src/utils/auth.ts или подобный
```

**Рекомендации:**
- 🔐 Используйте **сложный пароль** (минимум 12 символов)
- 🔄 Меняйте пароль **каждые 3 месяца**
- ❌ **Не делитесь** паролем
- ✅ Используйте **менеджер паролей** (1Password, LastPass)

### HTTPS

**Vercel:**
- ✅ HTTPS включен автоматически
- ✅ Сертификат обновляется автоматически
- ✅ Ничего делать не нужно

**VPS:**
```bash
# Проверка срока действия сертификата
sudo certbot certificates

# Ручное обновление (если нужно)
sudo certbot renew

# Автообновление настроено по умолчанию
```

### Защита от спама

**Уже реализовано:**
- ✅ Email размыт (требует клика)
- ✅ Телефон размыт (требует клика)
- ✅ reCAPTCHA можно добавить (опционально)

**Дополнительная защита (опционально):**

```bash
# Установите rate limiting на VPS
# Ограничивает количество запросов с одного IP
```

---

## ⚡ Оптимизация

### Производительность

**Что проверять:**
- ⏱️ **Скорость загрузки** - должна быть <2 сек
- 📦 **Размер страницы** - оптимально <500 KB
- 🖼️ **Изображения** - должны быть сжаты

**Инструменты:**
- https://pagespeed.web.dev/ - проверка скорости
- https://gtmetrix.com/ - детальный анализ
- Chrome DevTools (F12) → Network

### Кэширование

**Vercel:**
- ✅ CDN кэширование включено автоматически
- ✅ Статические файлы кэшируются на 1 год
- ✅ Оптимизация изображений автоматическая

**VPS (Nginx):**
```nginx
# Добавьте в конфигурацию Nginx
location ~* \.(jpg|jpeg|png|gif|ico|css|js)$ {
    expires 1y;
    add_header Cache-Control "public, immutable";
}
```

---

## 🔧 Частые задачи

### Изменить часы работы

1. Откройте https://colorlab.lv/admin
2. **Настройки** → **Часы работы**
3. Измените время
4. Сохраните

### Добавить новую услугу

1. **Настройки** → **Услуги**
2. Нажмите **"Добавить услугу"**
3. Заполните название на русском и латышском
4. Сохраните

### Изменить контакты

1. **Настройки** → **Контакты**
2. Обновите адрес, телефон, email, Instagram
3. Сохраните

### Экспорт записей

```javascript
// В консоли на /admin
const records = JSON.parse(localStorage.getItem('records') || '[]');
const csv = [
  ['Дата', 'Время', 'Имя', 'Телефон', 'Email', 'Услуга', 'Статус'],
  ...records.map(r => [
    r.date,
    r.time,
    r.name,
    r.phone,
    r.email,
    r.service,
    r.status
  ])
].map(row => row.join(',')).join('\n');

const blob = new Blob([csv], {type: 'text/csv'});
const url = URL.createObjectURL(blob);
const a = document.createElement('a');
a.href = url;
a.download = 'records.csv';
a.click();
```

### Очистка старых записей

**⚠️ ОСТОРОЖНО! Сделайте бэкап сначала!**

```javascript
// В консоли на /admin
const records = JSON.parse(localStorage.getItem('records') || '[]');

// Удалить записи старше 90 дней
const cutoffDate = new Date();
cutoffDate.setDate(cutoffDate.getDate() - 90);

const filtered = records.filter(r => {
  const recordDate = new Date(r.createdAt);
  return recordDate > cutoffDate;
});

localStorage.setItem('records', JSON.stringify(filtered));
console.log(`Удалено записей: ${records.length - filtered.length}`);

// Обновите страницу
location.reload();
```

---

## 🚨 Аварийное восстановление

### Сайт не работает

**1. Проверьте статус:**
- Vercel: https://vercel.com/status
- VPS: `ssh` → `pm2 status` → `sudo systemctl status nginx`

**2. Откатитесь на предыдущую версию:**

**Vercel:**
1. Зайдите в Dashboard
2. **Deployments**
3. Найдите последний рабочий деплой
4. Нажмите **"..."** → **"Promote to Production"**

**VPS:**
```bash
cd /var/www/colorlab
git log --oneline  # найдите последний рабочий коммит
git checkout COMMIT_HASH
npm run build
pm2 restart colorlab-booking
```

### Потеря данных

**Восстановите из бэкапа:**
1. Откройте https://colorlab.lv/admin
2. Откройте консоль (F12)
3. Вставьте код импорта (см. раздел "Импорт данных")
4. Выберите файл бэкапа
5. Обновите страницу

---

## 📞 Поддержка

**Проблемы с хостингом:**
- 🌐 hostnet.lv: https://www.hostnet.lv/support
- 📧 support@hostnet.lv

**Проблемы с Vercel:**
- 🌐 https://vercel.com/support
- 📚 https://vercel.com/docs

**Проблемы с кодом:**
- 📖 README.md в проекте
- 🔍 GitHub Issues (если используете)

---

## 📝 Журнал обслуживания

**Рекомендуется вести журнал:**

```
ДАТА       | ДЕЙСТВИЕ                        | РЕЗУЛЬТАТ
-----------|---------------------------------|------------------
2024-01-29 | Первый деплой на Vercel         | ✅ Успешно
2024-02-01 | Обновление дизайна футера       | ✅ Успешно
2024-02-05 | Добавление Instagram            | ✅ Успешно
2024-02-10 | Резервное копирование данных    | ✅ 150 записей
```

---

## ✅ Чек-лист ежемесячного обслуживания

- [ ] Проверить работу всех страниц
- [ ] Проверить отправку email
- [ ] Сделать резервную копию данных
- [ ] Проверить логи на ошибки
- [ ] Проверить скорость загрузки
- [ ] Проверить SSL сертификат (срок действия)
- [ ] Обновить пароль (если прошло 3 месяца)
- [ ] Очистить старые записи (опционально)
- [ ] Проверить актуальность контактов
- [ ] Обновить часы работы (если изменились)

---

**🎉 Ваш сайт под контролем!** 💪✨
