# Система хранения пароля администратора

## 📁 Где хранится пароль?

Пароль администратора хранится в файле `.password.json` в корневой директории проекта.

```
booking-app/
├── .password.json  ← Файл с паролем (создается автоматически)
├── .env.local
├── src/
└── ...
```

## 🚀 Как это работает?

### При первом запуске:

1. При первом входе в систему автоматически создается файл `.password.json`
2. В него копируется пароль из `.env.local` (переменная `ADMIN_PASSWORD`)
3. Файл имеет структуру:

```json
{
  "password": "ваш_пароль",
  "updatedAt": "2024-01-27T10:30:00.000Z"
}
```

### При смене пароля:

1. Заходите в **Админ панель → Настройки → Пароль**
2. Вводите текущий пароль и новый пароль
3. Нажимаете "Изменить пароль"
4. **Пароль меняется мгновенно** без перезапуска сервера!

### Fallback механизм:

Если файл `.password.json` не существует или поврежден:
- Система автоматически использует `ADMIN_PASSWORD` из `.env.local`
- Файл создается при следующем входе

## 🔐 Безопасность

### ✅ Что сделано:

1. **Файл `.password.json` добавлен в `.gitignore`**
   - Не попадает в Git репозиторий
   - Не коммитится случайно

2. **Локальное хранение**
   - Файл находится только на сервере
   - Нет доступа извне

3. **Автоматическое создание**
   - При первом запуске файл создается с паролем из `.env.local`
   - Нет необходимости вручную создавать файл

### ⚠️ Рекомендации:

1. **Установите права доступа к файлу:**
   ```bash
   chmod 600 .password.json
   ```

2. **Регулярно меняйте пароль** через интерфейс настроек

3. **Не удаляйте `.password.json` вручную** (если удалите, создастся заново с паролем из `.env.local`)

## 🛠️ Для разработки

### При деплое на сервер:

1. Убедитесь, что `.env.local` содержит `ADMIN_PASSWORD`
2. При первом запуске автоматически создастся `.password.json`
3. Можете удалить `ADMIN_PASSWORD` из `.env.local` (опционально)

### При работе в команде:

- Каждый разработчик имеет свой `.password.json`
- Файл не синхронизируется через Git
- Можно установить разные пароли на разных окружениях

## 💡 FAQ

**Q: Что если я забыл пароль?**
A: Удалите файл `.password.json` и в `.env.local` установите новый `ADMIN_PASSWORD`. При следующем входе файл создастся заново.

**Q: Можно ли менять пароль вручную в файле?**
A: Да, но лучше использовать UI в настройках. Формат файла простой - JSON с полем `password`.

**Q: Нужен ли перезапуск после смены пароля?**
A: Нет! Пароль читается из файла при каждой попытке входа.

**Q: Безопасно ли хранить пароль в файле?**
A: Да, если:
   - Файл добавлен в `.gitignore` ✅
   - Установлены правильные права доступа (600) ✅
   - Сервер защищен от несанкционированного доступа ✅
